  <refentry id="kacreateenv">

  <refmeta>
    <refentrytitle>deploy_cmd.conf</refentrytitle>
    <manvolnum>1</manvolnum>
  </refmeta>
  
  <refnamediv>
    <refname>deploy_cmd.conf</refname>
    <refpurpose>Configuration files for kadeploy</refpurpose>
  </refnamediv>
  

  <refsect1>
    <title>DESCRIPTION</title>


    <para>
      This file contains the appropriate reboot and remote console opening commands. 
      The kareboot and kaconsole tools get the commands to execute according to node given in parameter from it.
    </para>
    <para>
      Also, the user can find an example in the tools/cookbook folder with a brief description of the syntax to use.
    </para>
    <para>
      The syntax is <emphasis>host_name command_type command</emphasis> where possible command types are softboot, hardboot, deployboot or console.
    </para>

    <para>
      <command>softboot</command> the sofboot line is the software reboot command. Typically it looks something like <emphasis>ssh user@hostname /sbin/reboot</emphasis> 
    </para>

    <para>
      <command>deployboot</command> the deployboot line is the command to execute to reboot the node under the deployment kernel e.g. using the <emphasis>setup_pxe.pl</emphasis> utility in the <emphasis>boot/</emphasis> directory.
    </para>

    <para>
      <command>hardboot</command> hardboot is the hardware reboot command depending on special hardware equipment.
    </para>


    <para>
      <command>console</command> console is the command to open a remote console on a cluster node e.g. via serial port using the kermit software.
    </para>



  </refsect1>

  <refsect1>
    <title>COPYRIGHTS</title>
    <para>
      Copyright  2004 Laboratoire Id-Imag (http://www-id.imag.fr). This software is licensed under the GNU Library General Public License. There is NO warranty; not even for MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.
    </para>
  </refsect1>  

</refentry>
