  <refentry id="kacreateenv">

  <refmeta>
    <refentrytitle>kacreateenv</refentrytitle>
    <manvolnum>1</manvolnum>
  </refmeta>
  
  <refnamediv>
    <refname>kacreateenv</refname>
    <refpurpose> creates and registers new environment</refpurpose>
  </refnamediv>
  

  <refsect1>
    <title>SYNOPSIS</title>
    <programlisting>
kacreateenv 
   <command>-e | --environment</command>  environment_name 
   <command>-v | --version</command>      version 
   <command>-X | --exclude-from</command> file 
   <command>-z | --gzip</command>
   <command>--output-directory</command>  output_directory
   <command>--root-directory</command>    root_directory
   <command>--author</command>            email_address
   <command>--description</command>       "description"
   <command>--file-base</command>         file
   <command>--file-site</command>         file
   <command>--size</command>              size
   <command>--kernel-path</command>       kernel_path
   <command>--fdisk-type</command>        fdisk_type_number
   <command>--file-system</command>       file_system_type
      </programlisting>

  </refsect1>


  <refsect1>
    <title>DESCRIPTION</title>
    
    kacreateenv creates and registers a new environment from a running system one.
    
  </refsect1>
    

  <refsect1>
    <title>OPTIONS</title>    
    <command>-e,  --environment environment_name </command>
    the registration and output image name for the environment

    <command>-v,  --version version</command> 
    the version number of the environment (if needed) ; default is 1

    <command>-d,  --description "description"</command>
    A brief description of the environment
    <command>-a,  --author author_email </command>
    The author email address.

    <command>-fb, --filebase environment_image_path </command>
    The complete path to the environment image.

    <command>-ft, --filesite post-installation_file_path </command>
    The complete path to the post-installation file.

    <command>-s,  --size size (Mo) </command>
    The size of the environment ; in order to perform partition size check before a deployment.

    <command>-k,  --kernelpath kernel_path </command>
    The complete kernel path in the environment.

    <command>-fd, --fdisktype fdisk_type_number </command>
    The fdisk type ; default is 82.

    <command>-fs, --filesystem file_system_type </command>
    The file system type ; default is ext2.

    <command>--root-directory root_directory </command>
    Sets the root directory for the environment creation.

    <command>-X, --exclude-from file </command>
    Excludes files named into file from the generated environment.

    <command>-z, --gzip </command>
    Filters the archive through gzip.

    <command>--output-directory</command> output_directory
    Sets the output directory for the generated environment ; default is current directory.

  </refsect1>

  <refsect1>
    <title>EXAMPLE</title>
    <programlisting>      
      <command>kacreateenv</command>
            <command>-e </command>              toto_env 
            <command>--description</command>    "toto's special image" 
            <command>--author</command>         toto@fai.fr 
            <command>-fb</command>              file://home/images_folder/toto_env.tgz 
            <command>-ft</command>              file://home/images_folder/toto_spec_file.tgz 
            <command>--size</command>           650 
            <command>-k</command>               /boot/vmlinuz 
            <command>--root-directory</command> /home/toto_img/ 
            <command>-X</command>               /path/to/file_to_exclude.txt 
            <command>-z</command>               creates and registers the environment according to the parameters 
    </programlisting>
  </refsect1>
  
</refentry>