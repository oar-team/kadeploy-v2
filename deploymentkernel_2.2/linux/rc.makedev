#!/bin/bash
#set -x
NAME=makedev
SCRIPTNAME=rc.$NAME
. etc/rc.conf
. etc/$SCRIPTNAME.conf

cp tools/MAKEDEV $PREFS/dev
cd $PREFS/dev
chmod 700 ./MAKEDEV
if [ ! -b hda ]; then
	./MAKEDEV generic-i386 	
fi
cd -
